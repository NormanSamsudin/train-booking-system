import 'package:flutter/material.dart';

class CoachWidget extends StatelessWidget {
  const CoachWidget({super.key});

  @override
  Widget build(BuildContext context) {
    
    return Container(child: Text('coach'),);
  }
}